# LiveKit basic configuration
port: 7880
log_level: info

rtc:
  use_external_ip: true
  port_range_start: 50000
  port_range_end: 50050
  ice_servers:
    - urls:
        - stun:stun.l.google.com:19302

# Replace these with the generated values from the README step:
# Format: key: secret
keys:
  API_KEY_HERE: API_SECRET_HERE