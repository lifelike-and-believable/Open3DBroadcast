# Agent Operating Instructions

## Note about plugin "copies"

The plugin in ProjectSandbox/Plugins/Open3DStream is a symlink to the plugin in plugins/unreal/Open3DStream. They are not copies. You only need to edit the file in one place.
